import 'package:flutter/material.dart';

class Palette {
  static const MaterialColor FBN_BLUE = const MaterialColor(
    0xff003366, // 0% comes in here, this will be color picked if no shade is selected when defining a Color property which doesn’t require a swatch.
    const <int, Color>{
      50: const Color(0xff003366), //10%
      100: const Color(0xffb74c3a), //20%
      200: const Color(0xffa04332), //30%
      300: const Color(0xff89392b), //40%
      400: const Color(0xff733024), //50%
      500: const Color(0xff5c261d), //60%
      600: const Color(0xff451c16), //70%
      700: const Color(0xff2e130e), //80%
      800: const Color(0xff170907), //90%
      900: const Color(0xff000000), //100%
    },
  );

  static const MaterialColor CUSTOM_WHITE = const MaterialColor(
    0xffFFFFFF, // 0% comes in here, this will be color picked if no shade is selected when defining a Color property which doesn’t require a swatch.
    const <int, Color>{
      50: const Color(0xffffffff), //10%
      100: const Color(0xffffffff), //20%
      200: const Color(0xffffffff), //30%
      300: const Color(0xffffffff), //40%
      400: const Color(0xffffffff), //50%
      500: const Color(0xffffffff), //60%
      600: const Color(0xffffffff), //70%
      700: const Color(0xffffffff), //80%
      800: const Color(0xffffffff), //90%
      900: const Color(0xffffffff), //100%
    },
  );

  static const MaterialColor CUSTOM_YELLOW = const MaterialColor(
    0xffE8BF4E, // 0% comes in here, this will be color picked if no shade is selected when defining a Color property which doesn’t require a swatch.
    const <int, Color>{
      50: const Color(0xffffffff), //10%
      100: const Color(0xffffffff), //20%
      200: const Color(0xffffffff), //30%
      300: const Color(0xffffffff), //40%
      400: const Color(0xffffffff), //50%
      500: const Color(0xffffffff), //60%
      600: const Color(0xffffffff), //70%
      700: const Color(0xffffffff), //80%
      800: const Color(0xffffffff), //90%
      900: const Color(0xffffffff), //100%
    },
  );

  static const MaterialColor LAVENDAR_GREY = const MaterialColor(
    0xffE2E4E8, // 0% comes in here, this will be color picked if no shade is selected when defining a Color property which doesn’t require a swatch.
    const <int, Color>{
      50: const Color(0xffffffff), //10%
      100: const Color(0xffffffff), //20%
      200: const Color(0xffffffff), //30%
      300: const Color(0xffffffff), //40%
      400: const Color(0xffffffff), //50%
      500: const Color(0xffffffff), //60%
      600: const Color(0xffffffff), //70%
      700: const Color(0xffffffff), //80%
      800: const Color(0xffffffff), //90%
      900: const Color(0xffffffff), //100%
    },
  );

  static const MaterialColor FBN_GREEN = const MaterialColor(
    0xFF03BB18, // 0% comes in here, this will be color picked if no shade is selected when defining a Color property which doesn’t require a swatch.
    const <int, Color>{
      50: const Color(0xffffffff), //10%
      100: const Color(0xffffffff), //20%
      200: const Color(0xffffffff), //30%
      300: const Color(0xffffffff), //40%
      400: const Color(0xffffffff), //50%
      500: const Color(0xffffffff), //60%
      600: const Color(0xffffffff), //70%
      700: const Color(0xffffffff), //80%
      800: const Color(0xffffffff), //90%
      900: const Color(0xffffffff), //100%
    },
  );

  static const MaterialColor FBN_ORANGE = const MaterialColor(
    0xFFED682F, // 0% comes in here, this will be color picked if no shade is selected when defining a Color property which doesn’t require a swatch.
    const <int, Color>{
      50: const Color(0xffffffff), //10%
      100: const Color(0xffffffff), //20%
      200: const Color(0xffffffff), //30%
      300: const Color(0xffffffff), //40%
      400: const Color(0xffffffff), //50%
      500: const Color(0xffffffff), //60%
      600: const Color(0xffffffff), //70%
      700: const Color(0xffffffff), //80%
      800: const Color(0xffffffff), //90%
      900: const Color(0xffffffff), //100%
    },
  );
}
